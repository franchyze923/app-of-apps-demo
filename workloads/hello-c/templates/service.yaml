apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-c.fullname" . }}
  labels:
    {{- include "hello-c.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-c.selectorLabels" . | nindent 4 }}
